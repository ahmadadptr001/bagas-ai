# Sinkron dengan pyproject.toml [project].dependencies (sumber kebenaran).
playwright>=1.40.0
openai>=1.30.0
rich>=13.0.0
textual>=1.0.0
prompt_toolkit>=3.0.39
pyfiglet>=1.0.0
# /live dan tool take_screenshot.
Pillow>=10.0.0
# Suara kabar berbahasa Indonesia (opsional runtime: fallback System.Speech).
edge-tts>=6.1.0
# /voice — mikrofon dan pengenal lokal.
sounddevice>=0.4.6
# Mesin pengenal: Whisper lokal (faster-whisper), luring, tanpa API key.
faster-whisper>=1.0.0
# WebRTC lokal: noise suppression, AGC, VAD sebelum Whisper.
aec-audio-processing>=1.0.1
ddgs>=6.0.0
python-telegram-bot>=21.0
# Server MCP (`bagas-ai mcp`).
mcp>=1.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0
# Updater memvalidasi requirement per paket.
packaging>=23.0
# Sidebar "Sistem" (cpu/memory/disk).
psutil>=5.9.0
