openai>=1.30.0
rich>=13.0.0
pyfiglet>=1.0.0
# /live dan tool take_screenshot.
Pillow>=10.0.0
ddgs>=6.0.0
python-telegram-bot>=21.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
tenacity>=8.2.0
pydantic>=2.0.0
requests>=2.31.0
# Server MCP (`bagas-ai mcp`) — sasaran tepat untuk AI. Hanya perintah itu
# yang membutuhkannya.
mcp>=1.0
# Opsional: fitur CONNECTOR web-AI (/model kimi-web, qwen-web) — otomasi
# browser ke kimi.com / chat.qwen.ai. Setelah install, jalankan sekali:
#   playwright install chromium
playwright>=1.40.0
# Opsional: /voice — mikrofon (sounddevice merekam; SpeechRecognition adalah
# mesin CADANGAN lewat layanan Google lawas, bahasa id-ID).
sounddevice>=0.4.6
SpeechRecognition>=3.10.0
# /voice — mesin pengenal UTAMA: Whisper lokal (faster-whisper). Jauh lebih
# tepat dari Google lawas untuk suara beraksen/berjarak/berderau, luring, dan
# tanpa API key. Unduhan model pertama (small ±460 MB) terjadi otomatis saat
# /voice on pertama; sementara menunggu, pengenalan memakai Google.
faster-whisper>=1.0.0
