# Secrets
.env

# Agent working directory (file/kode yang dibuat agent)
workspace/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
.vscode/
.idea/

# Artefak build setuptools — JANGAN di-commit: build/lib jadi cache basi,
# pip ikut membungkusnya dan update tak pernah benar-benar terpasang.
build/
dist/

# Keluaran tool yang memang dibuat SAAT DIPAKAI, bukan bagian dari kode:
# web_preview & take_screenshot menulis PNG ke sini tiap kali dipanggil.
# Sempat ikut ter-commit dari uji end-to-end — repo bukan tempat menyimpannya.
screenshots/
